name: symbiflow-env
channels:
  - defaults
  - litex-hub
dependencies:
  - litex-hub::yosys
  - litex-hub::symbiflow-yosys-plugins
  - python=3.7
  - pip
  - pip:
    - -r ../common/requirements.txt
    - -r requirements.txt
